module doc_code_block;

<*
 Prose line here.

 ```c3
 fn int main(String[] args)
 {
 	bool help = false;
 	if (catch err = result)
 	{
 		return 0;
 	}
 }
 ```

 More prose that follows the code block.
*>
fn void doc_comment_code_block()
{
}
